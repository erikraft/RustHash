{
  "name": "rusthash-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix frontend --legacy-peer-deps",
    "build:wasm": "npx wasm-pack build rust --out-name hash_wasm --out-dir ../frontend/src/pkg --target web",
    "build:frontend": "npm run --prefix frontend build",
    "build": "bash build.sh",
    "dev": "npm run build:wasm && npm run --prefix frontend dev"
  },
  "devDependencies": {
    "wasm-pack": "^0.15.0"
  }
}
